```markdown
## 任务：把 `D:\trunk\test-sidepanel-host` 按“路径 B”改造成公司远程 Plugin Shell 页面

### 1. 背景与目标

当前目录 `D:\trunk\test-sidepanel-host` 是一个演示版 sidepanel host 工程，包含：

- `sidepanel-host.ts`
- `index.html`
- `company-app.html`

它现在的思路更接近“独立的 sidepanel-host.js 作为宿主”。

**现在要改目标架构**：

- **不再产出 / 不再依赖 `sidepanel-host.js`**
- 最终作为一个**公司远程 Plugin Shell 页面**运行
- 这个 Plugin Shell 页面会被 Chromium 侧边栏直接以 iframe 方式加载
- Chromium 侧边栏本地 `chrome://` 页面仍然是宿主，继续通过 `postMessage` 提供浏览器能力
- 公司远程页面内部保留自己的 UI / 数字人 / 状态管理
- 如果需要，Plugin Shell 页面内部还可以继续嵌一个“公司主系统 iframe”

也就是说，目标是把旧插件的 `background.js` 思路，**并入公司远程 shell 页面自身**，由它负责：

1. 与 Chromium 侧边栏父页面通信
2. 与内部业务 iframe 通信
3. 做协议转发和状态管理
4. 驱动数字人 UI / 业务 UI

### 2. 当前目录现状

当前目录结构大致如下：

```text
D:\trunk\test-sidepanel-host
  dist/
    sidepanel-host.js
  company-app.html
  esbuild.config.mjs
  index.html
  package.json
  sidepanel-host.ts
```

### 3. 目标改造方向：路径 B

改造成如下结构：

```text
Chromium sidebar
  -> 加载 company plugin shell page (index.html)
       -> shell page 内再加载 company-app.html
```

其中：

- `index.html`
  - 变成最终给 Chromium 加载的公司远程 shell 页面
  - 承担原来 `background.js` 的页面内替代职责
  - 负责：
    - 向父页面（Chromium sidebar）发送宿主请求
    - 接收宿主响应
    - 与内部 `company-app.html` iframe 通信
    - 做请求转发 / 响应转发 / 状态桥接
    - 展示 demo UI / 数字人 / 日志面板

- `company-app.html`
  - 作为内部业务 iframe
  - 不再直接和 Chromium sidebar 父页面通信
  - 只和外层 `index.html`（shell 页面）通信

- `sidepanel-host.ts`
  - 不再保留当前“创建 iframe + 绑定 NativeHostAPI”的角色
  - 应删除或废弃
  - 它的逻辑要并入 `index.html` 的页面脚本，或者拆成浏览器页面可直接引用的 `plugin-shell.ts`

### 4. 明确的职责边界

#### 4.1 外层 `index.html`（Plugin Shell）
它是整个远程页面的核心。

它要做两件通信：

**A. shell <-> Chromium sidebar 父页面**
- 使用 `window.parent.postMessage(...)`
- 发送请求给父页面
- 接收父页面返回的响应

**B. shell <-> 内部 `company-app.html` iframe**
- 使用 `iframe.contentWindow.postMessage(...)`
- 接收内部 iframe 发来的业务命令
- 转发给父页面
- 再把响应回传给内部 iframe

#### 4.2 内层 `company-app.html`
- 只把自己当作“业务 app 页面”
- 不直接感知 Chromium
- 不直接向 `window.parent.parent` 或 Chromium 发消息
- 只向它的直接父页面（也就是 shell）发消息

#### 4.3 绝对禁止
这份远程前端代码中，**禁止出现**：

- `AIAutomationClient`
- `mojo`
- `mojom-webui`
- `chrome://`
- `chrome.send`
- `sendWithPromise`
- `chrome.runtime`
- `chrome.tabs`
- `puppeteer`
- Node.js API

这是一个纯浏览器远程页面工程，只能使用：
- DOM
- iframe
- `window.postMessage`
- fetch / WebSocket
- 普通前端 JS / TS

### 5. 需要你做的具体改动

#### 5.1 重新定义运行结构
把 `index.html` 改造成：

- 外层 shell 页面
- 页面内嵌一个 `company-app.html` iframe
- shell 页面内维护通信日志
- shell 页面内维护与 Chromium 父页面的通信桥
- shell 页面内维护与 `company-app.html` 的转发桥

#### 5.2 `company-app.html` 改造
当前 `company-app.html` 里有这种逻辑：

- 通过 `parent.postMessage(...)` 发送命令
- 假设它的父页面就是宿主

现在要改成：

- 它的父页面是 `index.html` shell 页面
- 所以它仍然可以继续 `parent.postMessage(...)`
- 但是**消息协议语义上是发给 shell，而不是 Chromium**
- shell 再决定是否转发给 Chromium sidebar 父页面

你需要确保：
- `company-app.html` 不再假设自己直接连宿主
- 它只做业务命令发起与结果展示

#### 5.3 删除 / 停用 `sidepanel-host.ts`
- 不再保留当前 `SidePanelHost` 类模型
- 不再产出 `dist/sidepanel-host.js`
- 不再使用 `NativeHostAPI` 注入方式

如果你觉得有必要保留部分逻辑，请把它改造成一个**普通浏览器页面模块**，例如：
- `plugin-shell.ts`
- 由 `index.html` 直接 `<script type="module"> import './plugin-shell.ts' </script>`

但不要保留“宿主构造函数 + injected nativeAPI”这种模式。

#### 5.4 shell 页面需要实现一个 `callParentHost()` 方法
目标能力：

```typescript
async function callParentHost(command: string, payload?: any): Promise<any>
```

它做的事：

1. 生成 `request id`
2. 向 `window.parent.postMessage(...)` 发请求
3. 监听父页面响应
4. 根据 `id` 匹配响应
5. 成功时 resolve `payload`
6. 失败时 reject `error`

请求/响应协议保持如下格式：

**请求**
```typescript
{
  type: 'request',
  id: string,
  command: string,
  payload?: any,
  timestamp: number,
}
```

**响应**
```typescript
{
  type: 'response',
  id: string,
  command: string,
  payload?: any,
  error?: {
    code: string,
    message: string,
  },
  timestamp: number,
}
```

#### 5.5 shell 页面需要实现“内外双向转发”
shell 页面需要做：

- 监听内部 `company-app.html` iframe 发来的 `request`
- 把这个 `request` 转发给 Chromium 父页面
- 收到父页面 `response` 后，再回传给内部 iframe

也就是说：

```text
company-app.html
  -> shell(index.html)
  -> Chromium sidebar parent
  -> shell(index.html)
  -> company-app.html
```

#### 5.6 shell 页面要有严格 origin 校验
你需要补清楚以下三类 origin：

- `parentOrigin`
  - Chromium sidebar 父页面 origin
  - 开发时可能是 `chrome://ai-automation-side-panel.top-chrome`
- `appOrigin`
  - `company-app.html` 所在 origin
  - 本 demo 通常和 shell 同源
- 只接受白名单 origin 的消息

### 6. 建议改造后的文件结构

你可以按这个方向整理：

```text
D:\trunk\test-sidepanel-host
  index.html              # 最终 shell 页面
  company-app.html        # 内层业务 iframe
  plugin-shell.ts         # shell 通信桥逻辑（新增，推荐）
  package.json
  esbuild.config.mjs      # 改为编译 plugin-shell.ts
```

如果你觉得纯 HTML 内嵌脚本更方便，也可以不新建 `plugin-shell.ts`，直接把逻辑写进 `index.html` 的 `<script type="module">` 中。但推荐提取到独立 ts 文件。

### 7. build 要求

如果保留 TS 构建，请改成编译 `plugin-shell.ts`：

```js
await esbuild.build({
  entryPoints: ['plugin-shell.ts'],
  bundle: true,
  format: 'esm',
  platform: 'browser',
  target: ['es2022'],
  outfile: 'dist/plugin-shell.js',
});
```

然后在 `index.html` 中：

```html
<script type="module" src="./dist/plugin-shell.js"></script>
```

**注意**：
- 最终不再产出 `sidepanel-host.js`
- 最终交付的是整个远程页面目录/站点，而不是单个 host JS 文件

### 8. UI 要求

保留当前 demo 的这些价值：

- 外层 shell 日志面板
- 通信流向可视化
- 内层 `company-app.html` 的数字人 / demo UI
- 快捷命令按钮

但要把视觉表达改成“外层是 Plugin Shell，内层是 Company App”。

建议把页面中的标签文字改成更准确的语义：

- `SidePanel Host` -> `Plugin Shell`
- `Company App` 保留
- 架构图改成：
  - `Chromium Sidebar Parent`
  - `Plugin Shell`
  - `Company App iframe`

### 9. 输出要求

请直接修改 `D:\trunk\test-sidepanel-host` 下现有代码，使其成为一个完整可运行的“路径 B” demo。

完成后请输出：

1. 改了哪些文件
2. 每个文件承担什么职责
3. 最终通信链路说明
4. 本地运行方式（例如 `npm install && npm run build && node serve.mjs`）
5. 不要保留 `sidepanel-host.js` 作为最终宿主方案

### 10. 实现原则

优先遵守：

- 最小改动但结构正确
- 保留 demo 可视化价值
- 代码尽量清晰
- 不引入扩展 API / Mojo / puppeteer 依赖
- 不要写成新的 Chromium 宿主，而要写成**远程 Plugin Shell 页面**

请直接开始改代码，不要只给方案说明。
```