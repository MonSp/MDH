# Checklist

- [x] **C1**: `backend/protocol.py` 中定义了完整的会议模式消息类型枚举和数据模型（MeetingMessageType、AgentRole、MeetingAgentStatus、MeetingAgentInfo、MeetingTaskInfo、MeetingSummary）
- [x] **C2**: `src/modules/meetingProtocol.ts` 中定义了与后端一致的 TypeScript 接口类型
- [x] **C3**: `backend/meeting.py` 中 `MeetingSession` 类能正确创建 5 个角色 Agent（Planner、Executor、Monitor、Reviewer、Coordinator），每个 Agent 有唯一 id、name、role、status
- [x] **C4**: `MeetingSession.start()` 能初始化所有 Agent 并设置状态为 meeting
- [x] **C5**: `MeetingSession.stop()` 能停止所有 Agent、设置状态为 idle
- [x] **C6**: `backend/meeting_coordinator.py` 中 `MeetingCoordinator.decompose_task()` 能通过 LLM 将任务分解为结构化子任务列表（含名称、描述、优先级、依赖关系）
- [x] **C7**: `MeetingCoordinator.run_discussion()` 能按角色顺序生成各 Agent 的 LLM 讨论发言，并通过回调流式推送
- [x] **C8**: `MeetingCoordinator.assign_tasks()` 能根据 Agent 能力匹配分配子任务
- [x] **C9**: `backend/server.py` 正确处理 `start_meeting` 消息，创建 MeetingSession 并返回 `meeting_started`（含 meeting_id 和 agents 列表）
- [x] **C10**: `backend/server.py` 正确处理 `meeting_message` 消息，驱动 Agent 讨论并通过流式 `agent_message` 回传
- [x] **C11**: `backend/server.py` 正确处理 `task_assign` 消息，分配任务并返回 `task_assigned`
- [x] **C12**: `backend/server.py` 正确处理 `end_meeting` 消息，结束会议并返回 `meeting_ended`（含 summary）
- [x] **C13**: `backend/session.py` 中 `Session` 类已扩展 `meeting_session` 属性和 `meeting_mode` 标志
- [x] **C14**: `src/hooks/useMeetingSocket.ts` hook 能正确发送 startMeeting、sendMeetingMessage、assignTask、endMeeting 消息
- [x] **C15**: `useMeetingSocket` 能正确处理 `agent_message` 流式 delta 拼接，实现逐步打字效果
- [x] **C16**: `OfficeTeamMode.tsx` 已改用 `useMeetingSocket` hook，不再使用 mock 数据
- [x] **C17**: `MeetingChatPanel.tsx` 支持流式消息渲染（闪烁光标指示器）
- [x] **C18**: `TaskAssignPanel.tsx` 任务派发后等待后端确认再更新 UI
- [x] **C19**: `backend/tests/test_meeting.py` 测试全部通过（33/33），覆盖协议类型、MeetingSession 生命周期、任务管理、消息记录、完整生命周期
- [x] **C20**: 前后端协议类型一致，消息字段名和结构对齐
- [x] **C21**: 后端测试全部通过，前端 TypeScript 编译无新增错误（预存错误与本次变更无关）
