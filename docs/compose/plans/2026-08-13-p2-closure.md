# P2 遗留闭环计划（门禁通道感知 / hybrid 生产接线 / durable 读侧 / failover 归因 / 杂项）

> **For agentic workers:** REQUIRED SUB-SKILL: Use compose:subagent (recommended) or compose:execute. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 闭环 P2 阶段评审遗留项（2 Important + 其余 Minor），使 P2 零遗留后再进 P3。

**Architecture:** 五处补完：① 审查门禁信号按通道拆分（工具缺失匹配 `error`、真实失败在 `output`——消除误判 fail-open）；② per-agent hybrid 生产接线（CoordinatorConfig.hybridProfiles → createTeam 设置 `runtime.hybrid` + createExecutionConfig 对非法 profile 抛错/回退）并接线 cli；③ durable 读侧入口（`GET /api/workflow/executions` + resume 端点）+ per-execution 持久化加锁 + `persist_execution` 捕获 (OSError, TypeError, ValueError) + FAILED 恢复重跑测试；④ failover 归因收窄（`_run_agent_execution_loop` 的 `model.reply` 单点 try + `on_model_error` 回调，仅模型层异常标记）+ 门禁接入 `execute_and_review_task` + planner `issues` setdefault 加固。

**Tech Stack:** Python 3.11（backend）+ TypeScript（orchestrator）

## Global Constraints

- backend：`cd backend && /usr/bin/python3 -m pytest tests/<file> -q`（不加 `--timeout`）；全量 `tests/ -q`
- orchestrator：`cd orchestrator && npm test`（vitest；若陈旧 .js 遮蔽重现，确认 vitest.config.ts 的正则 alias 仍在）
- 不引入新依赖；不改 mock-sso；`backend/companion_log.json` 勿提交
- 基线（main@6cf4a54）：backend 891 passed / 1 skipped；orchestrator 118 passed；前端 1632 passed（worktree 缺未跟踪技能文件时 2 个 PRE-EXISTING 环境失败）
- 行号以实际代码为准（P0-P2 已使历史行号偏移）

---

### Task 1: 门禁通道感知匹配（消除真实失败被误判 fail-open）

**Covers:** P2-T2 最终复审 Important（工具缺失在 error 通道、真实失败在 output 通道）

**Files:** `backend/meeting_coordinator.py`、`backend/tests/test_meeting_coordinator_router.py`

**Interfaces:**
- Consumes: `_GATE_TOOL_MISSING_SIGNALS`（meeting_coordinator.py:860-869）、`_gate_check_unavailable`（:878-882）、`ToolResult.error/output`（tool_registry.py:66-71）
- Produces: `_gate_check_unavailable(error: str, output: str) -> bool`——通道感知：`("no module named", "no such file or directory", "command not found")` 仅匹配 `error`；`("no tests were collected", "no tests ran")` 匹配 `output`（两通道各自 `.strip().lower()`）

- [ ] **Step 1: 写失败测试**（追加到 test_meeting_coordinator_router.py `TestDeterministicGate`）：

```python
def test_gate_error_channel_only_prevents_real_failure_misjudgment():
    """真实失败输出（output 通道含 no such file or directory）不被误判为工具缺失"""
    from meeting_coordinator import MeetingCoordinator
    coordinator = object.__new__(MeetingCoordinator)
    # error 为空、output 含真实失败文本 → 判定为真实失败（非工具缺失）
    assert not MeetingCoordinator._gate_check_unavailable("", "FileNotFoundError: [Errno 2] No such file or directory: 'missing.csv'")
    # error 通道含工具缺失文本 → 判定为工具缺失
    assert MeetingCoordinator._gate_check_unavailable("[Errno 2] No such file or directory: 'pytest'", "")
    # output 通道含 no tests ran → 判定为无测试（跳过）
    assert MeetingCoordinator._gate_check_unavailable("", "no tests ran in 0.00s")
```

- [ ] **Step 2: 运行确认失败**：预期当前实现（合并 error+output 单文本匹配）对该用例返回错误结果。
- [ ] **Step 3: 实现**：`_gate_check_unavailable` 改为双通道签名；`_run_deterministic_gate` 中 lint/test 两分支分别以 `(result.error or "", result.output or "")` 调用；返回语义不变（True=工具缺失/无测试 → skipped）。
- [ ] **Step 4: 验证**：`cd backend && /usr/bin/python3 -m pytest tests/test_meeting_coordinator_router.py -q`；全量 `tests/ -q`。
- [ ] **Step 5: 提交**：`git add backend/meeting_coordinator.py backend/tests/test_meeting_coordinator_router.py && git commit -m "fix(review): channel-aware gate signals - tool-missing matches error, real failures in output"`

---

### Task 2: hybrid 生产接线 + profile 校验

**Covers:** P2-T5 复审 Important（runtime.hybrid 生产未赋值、ExecutionProfile 无校验）

**Files:** `orchestrator/src/team/coordinator.ts`、`orchestrator/src/team/coordinator.test.ts`、`orchestrator/src/toolkit/hybrid.ts`、`orchestrator/src/cli.ts`（或 `server.ts` 传递链）

**Interfaces:**
- Consumes: `CoordinatorConfig`（coordinator.ts:15-17 含 executorUrl/executorToken）、`createExecutionConfig(profile, {localWorkspace, remote})`（hybrid.ts:33-41）、`ExecutionProfile`、`TeamMember.runtime.hybrid`（router.ts:28-40 已读）
- Produces: `CoordinatorConfig.hybridProfiles?: Record<string, ExecutionProfile>`（role → profile）；`createTeam` 对 `hybridProfiles[roleId]` 存在的成员设置 `runtime.hybrid = { profile }`；cli `--profile`（MDH_PROFILE）默认应用到所有成员（`hybridProfiles` 为全角色映射）；`createExecutionConfig` 对未知 profile 抛 `Error("unknown ExecutionProfile")`（或回退 local-full + 日志，二选一，取抛错更安全）

- [ ] **Step 1: 写失败测试**（coordinator.test.ts 追加）：

```typescript
it('applies config hybridProfiles to member runtime.hybrid', async () => {
  const coordinator = makeCoordinator({ hybridProfiles: { executor: 'remote-brain-local-hands' } });
  await coordinator.execute('任务', ['executor', 'reviewer'], undefined, { executor: 'remote', reviewer: 'local' });
  const execMember = coordinator['team']!.members.find((m) => m.role === 'executor')!;
  expect((execMember.runtime as any).hybrid).toEqual({ profile: 'remote-brain-local-hands' });
});
```

（hybrid.ts 新增校验的测试：`expect(() => createExecutionConfig('bogus' as any, {})).toThrow()`——先读 createExecutionConfig 现签名，若它内部 `PROFILES[profile]` 取 undefined 则加显式校验。）

- [ ] **Step 2: 运行确认失败**：hybridProfiles 用例失败（无该配置字段）；校验用例失败（现不抛错）。
- [ ] **Step 3: 实现**：
  3a. `CoordinatorConfig` 加 `hybridProfiles?: Record<string, ExecutionProfile>`；`createTeam(roleIds, task, roleLocations, defaultRuntime, hybridProfiles = this.config.hybridProfiles)`；remote 分支 runtime 增加 `...(hybridProfiles?.[roleId] ? { hybrid: { profile: hybridProfiles[roleId] } } : {})`（或 local 分支亦允许——以 router.ts 读 hybrid 的语义为准，hybrid 在 remote/local 均可使）。
  3b. `cli.ts`：`--profile`/`MDH_PROFILE` 值传入 `startServer` → `CoordinatorConfig.hybridProfiles`（映射到全部角色，或仅当显式指定角色时——读 cli 现状，最小实现为全角色应用）。
  3c. `hybrid.ts createExecutionConfig`：profile 不在预置集与 'custom' 时 `throw new Error(...)`。
- [ ] **Step 4: 验证**：`cd orchestrator && npx vitest run src/team/coordinator.test.ts src/toolkit/hybrid.test.ts`；全量 `npm test`。
- [ ] **Step 5: 提交**：`git add orchestrator/src/team/coordinator.ts orchestrator/src/team/coordinator.test.ts orchestrator/src/toolkit/hybrid.ts orchestrator/src/cli.ts orchestrator/src/server.ts && git commit -m "feat(orchestrator): wire hybrid profiles into member runtime, validate ExecutionProfile"`

---

### Task 3: durable 读侧入口 + 持久化加固 + FAILED 重跑测试

**Covers:** P2-T1 复审 Minor（读侧未接线、并发写竞态、persist TypeError）、FAILED 重跑无测试

**Files:** `backend/workflow_engine.py`、`backend/server.py`、`backend/tests/test_durable_execution.py`

**Interfaces:**
- Consumes: `WorkflowEngine.load_all_executions`/`load_execution`（已实现）、`_executions`、`_running_tasks`
- Produces: `persist_execution` 捕获 `(OSError, TypeError, ValueError)`；`WorkflowEngine` 增加 `_persist_locks: Dict[str, asyncio.Lock]`（per-execution 串行化落盘，避免并行节点 last-writer-wins 竞态）；server REST：`GET /api/workflow/executions`（返回 `load_all_executions()`）与 `POST /api/workflow/resume/{id}`（`load_execution(id)` 后 `start_workflow(id)` 并 await，返回状态）；`test_reloaded_execution_reruns_failed_nodes`（恢复含 FAILED 节点 → 断言重跑）

- [ ] **Step 1: 写失败测试**（test_durable_execution.py 追加）：

```python
async def test_reloaded_execution_reruns_failed_nodes(tmp_path):
    """恢复含 FAILED 节点的工作流：FAILED 节点按重试语义重新执行（仅跳过 COMPLETED）"""
    engine1 = WorkflowEngine(persistence_dir=str(tmp_path))
    executed = []
    async def exec1(node, input_data):
        executed.append(node.node_id)
        return {"result": "ok"}
    engine1.register_node_executor("dept-frontend", exec1)
    engine1.register_node_executor("dept-backend", exec1)
    execution = engine1.create_workflow(_make_definition())
    execution.node_states["n1"] = WorkflowNodeStatus.COMPLETED
    execution.results["n1"] = {"result": "ok"}
    execution.node_states["n2"] = WorkflowNodeStatus.FAILED
    engine1.persist_execution(execution.execution_id)

    engine2 = WorkflowEngine(persistence_dir=str(tmp_path))
    engine2.register_node_executor("dept-frontend", exec1)
    engine2.register_node_executor("dept-backend", exec1)
    restored = engine2.load_execution(execution.execution_id)
    await engine2.execute_workflow(restored.execution_id)
    assert executed == ["n2"]  # FAILED 重跑，COMPLETED 跳过
```

（persist TypeError 用例：executor 返回含不可序列化对象的结果 → `persist_execution` 返回 False 不抛；并发加锁用例：并行 DAG 完成两节点后落盘文件包含两节点状态——用并发 executor 模拟。）

- [ ] **Step 2: 运行确认失败**：FAILED 重跑断言失败（现重跑 n1+n2）；TypeError 用例现抛异常。
- [ ] **Step 3: 实现**：
  3a. `persist_execution` 的 `json.dump`/`open` 包裹 `try/except (OSError, TypeError, ValueError) → return False + logger.warning`。
  3b. `WorkflowEngine` 加 `self._persist_locks: Dict[str, asyncio.Lock] = {}`；`persist_execution` 内 `lock = self._persist_locks.setdefault(execution_id, asyncio.Lock()); async with lock:`——注意 persist_execution 当前为同步方法：改为 `async def persist_execution` 会波及调用点；替代：同步方法内无法 await——改为在 `_execute_node`/`create_workflow` 等落盘调用点用 `asyncio.Lock` 串行化？同步 JSON 写本身原子（os.replace），竞态是"快照过期"而非损坏。**折中实现**：维持同步落盘 + 原子写（已实现），仅文档化"并行节点完成时 last-writer-wins 可致快照略旧，os.replace 保证文件完整"；若评审要求锁，则将 `persist_execution` 改为 async 并更新调用点为 `await`（约 7 处）。以评审结论为准，若实现 async 版本则调用点全部改 await。
  3c. server REST：读 `/api/workflow` 现有端点结构（server.py:2186+ 区域），新增两端点（复用 `_ok`/`workflow_execution_to_dict`）；resume 端点 `load_execution` 不存在时返回 `_fail`。
- [ ] **Step 4: 验证**：`cd backend && /usr/bin/python3 -m pytest tests/test_durable_execution.py tests/test_workflow_engine.py -q`；全量 `tests/ -q`。
- [ ] **Step 5: 提交**：`git add backend/workflow_engine.py backend/server.py backend/tests/test_durable_execution.py && git commit -m "feat(workflow): execution list/resume endpoints, robust persist, failed-node rerun test"`

---

### Task 4: failover 归因收窄 + 门禁接 execute_and_review_task + planner issues 加固

**Covers:** P2-T4 复审 I1（except 过宽归因）、M4 门禁缺口、P2-T2 复审 minor（planner issues setdefault）

**Files:** `backend/meeting_coordinator.py`、`backend/review_pipeline.py`、`backend/tests/test_meeting_coordinator_router.py`、`backend/tests/test_review_pipeline.py`

**Interfaces:**
- Consumes: `_run_agent_execution_loop(model, prompt, agent_toolset, max_tool_rounds)`（meeting_coordinator.py:189-232）、`_execute_workflow_node` except、`_mark_model_failed(role)`、`execute_and_review_task` review 调用、`_generate_structured_feedback`（review_pipeline.py:251+）
- Produces: `_run_agent_execution_loop` 增加 `on_model_error: Optional[Callable[[], None]] = None`——`model.reply` 包 try/except，异常时先 `on_model_error()` 再 re-raise；`_execute_workflow_node` 传 `on_model_error=lambda: self._mark_model_failed(role)` 并删除 except 中的直接调用（归因收窄：仅模型层异常标记）；`execute_and_review_task` 的 review 调用传入 `gate_result`（复用 `_run_deterministic_gate`）；`_generate_structured_feedback` 中 `result["issues"].append` 前 `result.setdefault("issues", [])`

- [ ] **Step 1: 写失败测试**：
  1a. meeting_coordinator_router：`_run_agent_execution_loop` 中 `model.reply` 抛异常时 `on_model_error` 被调用、且工具层异常（`agent_toolset.write_file` 抛错）不触发 `on_model_error`；`_execute_workflow_node` 模型异常 → `_mark_model_failed` 被调用。
  1b. review_pipeline：`execute_and_review_task` 门禁路径——模拟 coordinator 调用（或验证 review 接收 gate_result 的既有测试扩展）。
  1c. `_generate_structured_feedback` 在 `gate_result` 失败且 `result` 无 `issues` 键时不 KeyError（构造 planner 返回无 issues 的场景或用现有 fallback 路径）。
- [ ] **Step 2: 运行确认失败**：模型层异常未触发回调（现无该参数）；planner 无 issues 时 KeyError 复现。
- [ ] **Step 3: 实现**：按 Interfaces 三处修改；`execute_and_review_task` 门禁：在该方法内 `gate_result = self._run_deterministic_gate(self._workspace.root_path if self._workspace else None)` 并传入 review（注释更新，删除"暂不接门禁"声明）。
- [ ] **Step 4: 验证**：`cd backend && /usr/bin/python3 -m pytest tests/test_meeting_coordinator_router.py tests/test_review_pipeline.py tests/test_structured_feedback.py -q`；全量 `tests/ -q`。
- [ ] **Step 5: 提交**：`git add backend/meeting_coordinator.py backend/review_pipeline.py backend/tests/test_meeting_coordinator_router.py backend/tests/test_review_pipeline.py && git commit -m "fix(resilience): attribute model failures precisely, gate execute_and_review_task, harden issues merge"`

---

## 自检备注

- **Spec coverage**：T1 → P2-T2 Important 残余；T2 → P2-T5 Important 残余；T3 → P2-T1 Minor 组；T4 → P2-T4 I1/M4 + P2-T2 minor。对应分析文档 P3 表"评审遗留"行的闭环。
- **明确不做的**：failover 扩展到 decompose/review/discussion 全部消费点（成本高、收益低，各消费点已有 fallback——维持现状并在 `_mark_model_failed` docstring 注明覆盖范围）；hybrid 按角色差异化 profile 的 UI 配置（本期 cli 全角色应用即可）。
- **环境**：main@6cf4a54 基线；worktree 缺未跟踪技能文件致 2 个 PRE-EXISTING 失败。