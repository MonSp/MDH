# v1.2.2 架构治理 + TS 能力补齐 实施计划

> **版本**: v1.2.2
> **日期**: 2026-08-18
> **范围**: TS 端安全补齐 + 能力补齐，Python 端架构治理

---

## A 线：TS 安全补齐（P0）

### A1. Shell 命令安全
- [ ] **添加 shell 命令黑名单** — `orchestrator/src/toolkit/local.ts` 添加危险命令拦截（rm -rf /、curl|bash、eval、reverse shell 等）
- [ ] **添加 shell 命令白名单** — 限制可执行命令范围
- [ ] **路径遍历保护加固** — `orchestrator/src/toolkit/local.ts` 添加 realpath 校验

### A2. 工具参数校验
- [ ] **添加工具参数校验** — `orchestrator/src/agent/tools.ts` 执行前校验 required 参数
- [ ] **添加工具超时** — 每个工具独立超时（非仅 bash）

### A3. TS 端 LLM 安全
- [ ] **添加 LLM 调用超时守卫** — 对齐 Python 端 llm_guard.py 的 fail-closed 策略

---

## B 线：TS 能力补齐（P1）

### B1. LLM 提供商扩展
- [ ] **添加 DashScope/Qwen 支持** — `orchestrator/src/llm/` 新增 dashscope.ts
- [ ] **添加 Gemini 支持** — `orchestrator/src/llm/` 新增 gemini.ts
- [ ] **添加 Moonshot 支持** — `orchestrator/src/llm/` 新增 moonshot.ts
- [ ] **添加 xAI/Grok 支持** — `orchestrator/src/llm/` 新增 xai.ts
- [ ] **更新 LLMConfig 类型** — 扩展 provider 枚举
- [ ] **更新 RouterFactory** — 根据 provider 选择正确的 LLM 客户端

### B2. 思维链流式支持
- [ ] **添加 thinking block 解析** — `orchestrator/src/llm/openai.ts` 解析 reasoning_content 字段
- [ ] **添加 thinking 事件推送** — 推送 thinking_block_start/delta/end 事件到前端
- [ ] **前端 thinking 展示** — `MeetingChatPanel` 添加思维链折叠展示

### B3. HITL 确认流
- [ ] **添加确认请求事件** — `orchestrator/src/agent/role-agent.ts` 检测危险操作时发送 confirm_request
- [ ] **添加确认响应处理** — 处理 confirm_result 消息
- [ ] **集成到工具执行循环** — 工具执行前检查是否需要用户确认

### B4. 技能渐进加载
- [ ] **实现 L0 索引** — `orchestrator/src/skill/loader.ts` 添加轻量索引（~50 tokens/skill）
- [ ] **实现 L1-L3 按需加载** — 根据任务匹配度渐进加载指令/参考/脚本
- [ ] **添加技能到任务匹配** — 关键词匹配自动选择相关技能

---

## C 线：Python 架构治理（P2）

### C1. server.py 继续拆分
- [ ] **拆分 WebSocket handler 剩余部分** — 将 43 个消息类型中的长 handler 进一步提取
- [ ] **拆分 REST 端点** — 将内联端点迁移到 Router 模块（当前 5 个 Router 已启用但内联端点仍保留）

### C2. meeting_coordinator 继续拆分
- [ ] **提取讨论流程** — `run_discussion` 及相关方法提取到独立模块
- [ ] **提取投票流程** — 投票相关方法提取到独立模块
- [ ] **提取审查流程** — 审查相关方法提取到独立模块

### C3. 测试覆盖率提升
- [ ] **server.py 端点测试** — 补充 REST 端点测试（当前仅 35 个）
- [ ] **meeting_coordinator 测试** — 核心编排流程测试
- [ ] **组件测试** — SkillMarketplace、RoleEditorPanel 等未测试组件

---

## 执行顺序

1. **A1-A3** → TS 安全补齐（最高优先级）
2. **B1** → LLM 提供商扩展（影响最大）
3. **B2-B3** → 思维链 + HITL（用户体验提升）
4. **B4** → 技能渐进加载（性能优化）
5. **C1-C3** → Python 架构治理（持续推进）

---

## 验收标准

- [ ] TS 端 shell 命令安全测试通过
- [ ] TS 端 9 个 LLM 提供商全部可用
- [ ] TS 端思维链流式展示正常
- [ ] TS 端 HITL 确认流正常工作
- [ ] 所有现有测试通过（后端 1285+，前端 1662+）
- [ ] CI 全部通过
